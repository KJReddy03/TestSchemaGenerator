name: json_hive_generator
description: A code generator that reads JSON schema files and generates Hive database models automatically.
version: 1.0.0

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  build_runner: ^2.4.0
  hive_generator: ^2.0.1
  test: ^1.24.0
